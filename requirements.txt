# Core dependencies (always required)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# OCR dependencies (optional - only needed if RUN_OCR=True)
# Uncomment these lines if you want to run the full OCR pipeline:
# pdf2image>=1.16.0
# opencv-python-headless>=4.8.0
# pypdf>=3.15.0
# pytesseract>=0.3.10

# Note: If running OCR on Linux, you'll also need:
# sudo apt-get install poppler-utils
